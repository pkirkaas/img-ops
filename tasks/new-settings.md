# Task: Create new settings/configuration widget, based on previous work

The new SettingsConfig widget should allow the editing of a settings/configuration profile by name.
It should use the `selected_paths_widget` to set/get the paths
It should allow editing of the configuration name, description, similarity method, and similarity percentage
It should be loaded by the "Active Config" select control at the top of the application window
It should have an "OK" button to run validation checks and persist config changes to the config file
Put the new SettingsConfig widget in the main app window, where the "image_viewer.py" is currently. We will use that later.
