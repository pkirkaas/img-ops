# Task: Create Main App Window GUI
Create a main window application frame in ./src/img_ops/gui/windows/main_window.py with the PySide6 QT library. Suggest installation of additional libraries/utilities as appropriate.

The main window should be a shell/frame that can contain arbitrary added widgets later

The main app window should be resizable, with an initial size of size of 900 x 400 pixels, or equivalent units.

The main app window should be a standard application type window with a configurable menu bar.

Create the configurable menu widget if appropriate

The poetry script `start_gui = "img_ops.gui.main:run_gui"` in pyproject.toml should launch the application window.

Ask any clarifying questions as required.